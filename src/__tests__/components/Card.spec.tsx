import React from 'react';
import { render, RenderOptions } from '@testing-library/react';

import MainPage from '../../pages/MainPage';

// Testeando a página Main
describe('Maim Page Tests', () => {
  it('should render a div on the screen', () => {

    const { ge  } = render(<div />);

    const {  } = React

  });
});
